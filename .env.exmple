# ~/.config/ganaterm/.env
# 这是ganaterm的环境配置文件示例
# 复制此文件为.env并填入你的API密钥

# API密钥配置
OPENAI_API_KEY=   # OpenAI API密钥，支持GPT-4等模型
DEEPSEEK_API_KEY= # DeepSeek API密钥，支持deepseek-chat等模型
XAI_API_KEY=      # xAI API密钥，支持Grok模型

# 代理设置 (可选)
# HTTP_PROXY=http://127.0.0.1:7890
# HTTPS_PROXY=http://127.0.0.1:7890

# 显示效果设置
USE_TYPEWRITER=true    # 是否启用打字效果
TYPING_SPEED_WPM=256   # 打字速度 (每分钟字数)
USE_MARKDOWN=true      # 是否启用Markdown渲染 (需要安装rich库)

# 终端兼容性设置 (markdown渲染代码块颜色好像有问题？)
# 如果你使用zsh并且代码块显示有问题，可以尝试以下设置:
# TERM=xterm-256color   # 设置终端为256色模式
# USE_MARKDOWN=false    # 如果渲染效果不好，可以完全禁用Markdown
# 或者在 ~/.zshrc 中添加: export TERM=xterm-256color

# 其他
# 使用过程中如果出现：
# 是否写入文件file_1746867934.py?(y/n/e/rnm) y:写入 n:丢弃 e:显示内容 rnm:重命名
# (rnm成刚才的文件名)
